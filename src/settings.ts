document.addEventListener('DOMContentLoaded', () => {
    const toriSlider = document.getElementById('tori_slider') as HTMLInputElement;
    toriSlider.checked = true;

    const matekSlider = document.getElementById('matek_slider') as HTMLInputElement;
    matekSlider.checked = true;

    const infoSlider = document.getElementById('info_slider') as HTMLInputElement;
    infoSlider.checked = true;

    const bioszSlider = document.getElementById('biosz_slider') as HTMLInputElement;
    bioszSlider.checked = true;

});
